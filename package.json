{
  "name": "sample-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ch-01 - model only: one call behind a swappable provider seam",
  "engines": {
    "node": ">=18.0.0"
  },
  "bin": {
    "agent": "./bin/agent.js"
  },
  "scripts": {
    "start": "node bin/agent.js",
    "test": "node --test"
  }
}
